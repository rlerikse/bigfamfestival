# Big Fam 1.4.0 — Release Notes

## Under the Hood
- **Map points-of-interest API:** added a real backend endpoint (`GET /map/pois`) that serves festival map points of interest, replacing placeholder data. This is groundwork for richer, server-managed map content — no visible change on its own.

---

### Store-Ready Short Form (≤500 chars, for App Store / Play "What's New")

New in 1.4.0: behind-the-scenes groundwork for server-managed map points of interest.

---

_Technical detail: see [CHANGELOG.md](../../CHANGELOG.md) → 1.4.0._
