# Decisions — Big Fam Festival (repo memory)

### [2026-08-09] Jira space closed — specs are the system of record
The original Atlassian Jira space (`eriksensolutions.atlassian.net`) is **closed**. Every `**Jira**` link in `specs/BFF-*/spec.md` is **broken** and kept for reference only. The spec files themselves are now the last surviving point of reference for all BFF stories. When updating specs, **annotate (add verification banners), never delete** original content.
— discovered during spec status verification pass

### [2026-08-09] Authentication is Firebase Auth (JWT/bcrypt removed)
Backend verifies Firebase ID tokens via `FirebaseAuthGuard` (`admin.auth().verifyIdToken(token, true)`); mobile uses `firebaseAuthService.ts`. Legacy NestJS Passport/JWT + bcrypt is gone (`auth.service.ts`: "Legacy bcrypt/JWT operations have been removed"). BFF-4's spec body still describes the old JWT/bcrypt design (historical). Migration tracked in BFF-50 (~70%; legacy-user import script + `password` field purge unverified).
— discovered during spec status verification pass

### [2026-08-09] Map is Mapbox-only; HERE de-scoped; what3words never built
`@rnmapbox/maps` + `mobile/src/screens/MapScreen.tsx` is the shipped map. BFF-35 proposed a Mapbox/HERE hybrid but **no HERE SDK** was ever added. BFF-32 what3words is a **mock only** (`mapService.getWhat3WordsAddress()` returns random words); no package/API key — treat as de-scoped/obsolete.
— discovered during spec status verification pass

### [2026-08-09] Email provider is SendGrid
`backend/src/config/sendgrid/` + `@sendgrid/mail`, used by `backend/scripts/email-blast.ts`. "Custom SMTP" (BFF-52) effectively means SendGrid-as-provider. Firebase Auth transactional emails are NOT yet routed through custom SMTP; branded templates + SPF/DKIM/DMARC outstanding.
— discovered during spec status verification pass

### [2026-08-09] BFF-128 clarify: genre filter options = full day-independent 2026 lineup
Genre filter defect (Issue #185): `genreOptions` (`ScheduleScreen.tsx:L363`) derives from the separate Firestore `genres` collection + a hardcoded `sampleGenres` fallback, decoupled from the actual lineup. Fix (human-clarified): derive `genreOptions` from ALL loaded `events` (every day's already-`genreService`-enriched artist genres), producing one distinct, day-independent list that is STABLE across pull-to-refresh and day change. Do NOT derive from day-filtered `filteredEvents` (would shrink per day). The genre filter is ALREADY multi-select (`selectedGenres: string[]`, default `['all']`) — no UI change. Filtering behavior + selection persistence stay UNCHANGED ("the filtering works, just the wrong genres"); do NOT touch the filter reducer's day-change reset. Earlier auto-reset-to-All-Genres direction was dropped (spec DR-3) — a stable full-lineup list never makes a selection vanish. `event.genres` array takes precedence over single `event.genre`, mirroring the existing matcher (L574-581).

### [2026-08-09] BFF-128 verify: full verification PASS, 0 findings
Verified all 3 changed/added files (`scheduleUtils.ts` + `ScheduleScreen.tsx` modified, `scheduleUtils.genre.test.ts` new) against spec.md (FR-001..FR-009), plan.md (Decision 1/2), and all 5 DRs. `deriveGenreOptions`'s array/single-value precedence exactly mirrors the existing filter-matching logic at `ScheduleScreen.tsx:496-500` — confirmed identical, including the present-but-empty-array-takes-precedence edge case (dedicated test). Firestore `genres` collection read, `fetchGenres`, `sampleGenres`, and both invoking `useEffect`s fully removed with zero remaining references (grep-confirmed) — no dead code. Filter reducer, `selectedGenres`/`selectedDay`/`selectedStages`/`showMySchedule`, and the day/stage/My-Schedule/horizontal-schedule filtering `useMemo` (L493-520) confirmed byte-for-byte untouched, validating DR-3/FR-009. No scope drift — only the 3 declared files touched (plus expected tasks.md/.workflow-state.json bookkeeping). Independent re-run: 6/6 tests pass, `tsc --noEmit` clean, lint 0 errors (3 pre-existing unrelated non-null-assertion warnings at L385). Full report: `specs/BFF-128-schedule-genre-filter-lineup/verification-report.md`.
— discovered during BFF-128 /blue.clarify (pipeline)

### [2026-08-09] BFF-128 shipped — merged to dev, iOS+Android verified
BFF-128 pipeline complete: PR #234 merged into `dev` (merge commit `a00e3a4`; feature commit `df05bcb`). Manual native spot-check performed on BOTH iOS and Android (2026-08-09) — genre filter shows only lineup-represented genres, each yields results, list stable across refresh/day change, no regressions in day/stage/My-Schedule filters. All 7 acceptance criteria + T005 verified. Implementation report updated to "verification confirmed on iOS and Android."
— discovered during BFF-128 post-merge verification

### [2026-08-09] BFF-127 clarify: 3 Technical decisions auto-resolved, 0 human escalations
Run 1/5 under `/blue.clarify` pipeline mode (spec complexity MODERATE, score 5). Encoded as DR-1/DR-2/DR-3 in `specs/BFF-127-schedule-list-scroll-performance/spec.md`: (1) FlatList tuning — `initialNumToRender=10`, `maxToRenderPerBatch=10`, `windowSize=7`, `removeClippedSubviews=Platform.OS==='android'` (from `75`/`20`/`15`/`false`); (2) clock-decoupling strategy — derive a per-event display state (`'upcoming'|'live'|'completed'`) via a pure helper and gate `React.memo` re-render on that derived value rather than per-card timers or context-selector subscriptions; (3) extract `getEventDisplayState(event, nowMs)` into `mobile/src/utils/` as the single derivation source, satisfying FR-010's existing testability mandate. None of the 3 explicitly flagged behavior-defining risks (live/countdown cadence, Android blank-row trade-off, initial-mount visual change) needed human escalation — each was already resolved by existing FR/AC text plus code grounding: `EventCard.tsx`'s LIVE glow is already `useNativeDriver: true` (independent of JS re-renders, no countdown text exists); FR-006 already requires cadence-preserving "next available schedule-clock update"; AC US2.2 already hard-requires no blank rows regardless of technique; `getItemLayout`'s fixed 112px rows mean no new loading UI from a lower `initialNumToRender`.
— discovered during BFF-127 /blue.clarify (pipeline)

### [2026-08-09] Ticketing is disabled — BFF-27/BFF-38 blocked
`TicketsModule` is commented out in `backend/src/app.module.ts`. QR ticket display (BFF-27) and QR scanner gate entry (BFF-38) are unstarted and blocked until ticketing is revived. Vendor dashboard (BFF-31) is a mock admin UI shell only. Medical emergency (BFF-36) is unbuilt (only an "emergency" notification label exists) — safety-critical, needs a fresh spec if revived.
— discovered during spec status verification pass

### [2026-08-09] Archived unbuilt specs to specs/_archive/
Moved BFF-27, BFF-31, BFF-32, BFF-33, BFF-38 (all Not Implemented) into `specs/_archive/` via `git mv`, with an index at `specs/_archive/README.md`. Content preserved as a restart point. BFF-36 (medical) intentionally kept under `specs/` — unbuilt but safety-critical.
— done during spec archival

### [2026-08-09] Constitution audit --update: 5 fixes applied (v1.3.0 → v1.4.0)
Ran `/blue.constitution --audit --update`. Relaxed §XII "No Ticket = No Spec" (Jira space closed — specs are now the system of record, ticketless naming allowed going forward). Refreshed §XIV Required Workflows list (removed `sync-spec-context.yml`, added the 5 real workflows: release-please, android-deploy, ios-deploy, ci-gate, branch-name-check, prune-stale-branches). Removed false `deploy-functions.yml` claim from §XVI (no such workflow exists — Functions deploy manually). Corrected §VII password-length claim (Firebase default is 6 chars, not 8; unenforced). Added a "Known Deviations" section cross-referencing 3 tracked code-vs-rule violations (CORS wildcard default, unbounded Firestore reads, Sentry ErrorBoundary gap) to the hardening audit doc, so the constitution stops silently overclaiming compliance. Confirmed accurate (no drift): multi-tenancy (TenantMiddleware/FESTIVAL_ID do exist), docs/MOBILE_RELEASE_GUIDE.md + RELEASE_CHECKLIST.md exist, no `password` field persisted in Firestore.
— discovered during `/blue.constitution --audit --update`

### [2026-08-09] BFF-124 schedule scroll-sync spec is GitHub-sourced
BFF-124 documents a net-new mobile UI bug fix for vertical-scroll synchronization in `HorizontalScheduleView`. GitHub Issue #187 is the source of truth because Jira is closed; the draft belongs at `specs/BFF-124-horizontal-schedule-vertical-scroll-sync/` on `bugfix/BFF-124-vertical-scroll-sync`. The defect path records horizontal X only, leaving vertical Y uncaptured and unrestored across remounts.
— discovered during `/blue.specify BFF-124`

### [2026-08-09] BFF-124 clarify resolved 3 ambiguities in-place, complexity SIMPLE (score 4)
Ran `/blue.clarify` (attempt 1, autonomous pipeline mode). No human escalation needed. Resolved from direct code evidence rather than interactive questions: (1) new vertical `onScroll` listener uses `scrollEventThrottle={16}`, matching the existing horizontal body `ScrollView` convention at `HorizontalScheduleView.tsx:L467`; (2) FR-006 re-scoped from an implied "make the ruler/Now-indicator vertical-viewport-aware" requirement to a no-regression constraint — both are horizontal-axis-only visuals today (ruler `translateX` bound to `scrollX`; the "Now" line spans the full grid height independent of Y) and the fix must not add new Y-based positioning, only avoid desync/stale-render from the new vertical tracking; (3) confirmed User Story 2 AC3's "existing remount-key discipline" is the real `scrollResetKey` state counter (`HorizontalScheduleView.tsx:L289/L319/L433/L462`) that already forces a clean remount of both body `ScrollView`s via React `key` on genuine content/day changes — vertical restoration reuses this same mechanism. Flagged the exact code line for `/blue.plan`/`/blue.implement`: `L313` currently hardcodes `y: 0` when preserving X across a remount and must instead carry a clamped Y per FR-005.
— discovered during `/blue.clarify BFF-124` (attempt 1)

### [2026-08-09] BFF-128 Schedule genres must derive from the loaded lineup
GitHub Issue #185 is the authoritative source for BFF-128 because Jira is closed. `ScheduleScreen.tsx` currently renders `genreOptions` from a standalone Firestore `genres` collection and a static fallback, while the event filter already matches `event.genres` with an `event.genre` fallback. The BFF-128 draft requires dynamically deriving, deduplicating, and sorting options from current loaded events so every offered genre yields at least one schedule result.
— discovered during `/blue.specify BFF-128`

### [2026-08-09] BFF-128 plan generated — plan.md + tasks.md, analyze PASS
Ran `/blue.plan BFF-128` from the approved (Checkpoint 1) spec. Generated plan.md (skipped research.md/data-model.md/contracts — SIMPLE complexity, no unknowns/data model/API change) and tasks.md (5 tasks / 3 phases, TDD-first). Reused the BFF-124 precedent: a pure `deriveGenreOptions(events)` helper added to `mobile/src/utils/scheduleUtils.ts` (zero RN/Expo imports) so it can be unit-tested with plain Jest, avoiding the Expo SDK 54 font-asset transform failure that breaks any test importing `ScheduleScreen.tsx`. `analyze` ran PASS: 0 CRITICAL/HIGH findings, full FR-001..FR-009 coverage, DR-3/DR-5 consistency verified directly against `ScheduleScreen.tsx` source (confirmed `stageOptions` already uses the same full-`events`-keyed, day-independent pattern). 2 LOW cosmetic findings only (a task story-tag label, test-filename convention). Artifacts staged via `git add`, not committed — pipeline owns the signed commit after Checkpoint 2.
— discovered during `/blue.plan BFF-128`

### [2026-08-09] BFF-127 Schedule list performance spec is GitHub-sourced
GitHub Issue #186 is authoritative because Jira is closed. The list-view `FlatList` in `ScheduleScreen.tsx` renders 75 cards at initial mount and passes the ten-second `FakeClockContext` value through its card renderer, invalidating all mounted cards on every tick. The BFF-127 draft requires a visible-row render budget, bounded Android row retention, and card-local time-state updates. Device profiling is required for mount/scroll evidence; any unit-testable time-state logic must be extracted to a side-effect-free `mobile/src/utils/` helper because Jest cannot import the icon/image-bearing Schedule components under Expo SDK 54.
— discovered during `/blue.specify BFF-127`

### [2026-08-09] BFF-127 clarify confirmation pass (attempt 2/5) — CLEAN
Re-scanned the updated `spec.md` after attempt 1 auto-resolved DR-1 (FlatList tuning: `initialNumToRender=10`, `maxToRenderPerBatch=10`, `windowSize=7`, Android-only `removeClippedSubviews`), DR-2 (clock decoupling via a derived per-event display state gating `React.memo`), DR-3 (`getEventDisplayState(event, nowMs)` pure helper). Full 10-category ambiguity taxonomy re-checked: no remaining Behavior-defining or Technical gap found, no `[NEEDS CLARIFICATION]`/TODO markers left. SC-002/SC-003 "measurable reduction" performance thresholds are intentionally deferred to manual device profiling (Validation Plan) per explicit pipeline directive — not treated as a blocking ambiguity. FR-009's "favor correct scroll behavior" fallback (getItemLayout/height-mismatch handling) is a technical implementation choice correctly left for `/blue.plan`, not a spec-level gap. `.workflow-state.json` updated: `steps.clarify.status="clean"`, `clarification_attempt=2`.
— discovered during `/blue.clarify BFF-127` (attempt 2, pipeline confirmation pass)

### [2026-08-09] BFF-127 plan generated — plan.md + research.md + tasks.md, analyze PASS
Ran `/blue.plan BFF-127` from the approved (Checkpoint 1) spec. Continued spec.md's DR-1/DR-2/DR-3 with three new plan-stage decisions: DR-4 (`getEventDisplayState` added to existing `scheduleUtils.ts`, built on the module's existing `isEventLive`/`getEventEndMs` exports rather than a new file or a third reimplementation of the start/end/midnight/2h-fallback math — `EventCard.tsx` already had its own inline copy of this logic); DR-5 (critical cross-consumer catch — `grep_search "currentTime"` across `mobile/src/**` found `EventCard` is ALSO rendered by `mobile/src/components/LiveUpcomingEvents.tsx`, a Home-screen widget out of scope per FR-008/Non-Goals, which spec.md never mentioned; resolved by making the new `displayState` prop OPTIONAL/backward-compatible so that consumer needs zero changes and keeps its exact current Date-math behavior — `HorizontalScheduleView.tsx` was also checked and does NOT import `EventCard`, confirmed unaffected); DR-6 (the `currentTime` value fed to `EventCard` for the `timeUntilStart` countdown text is rounded to the minute boundary to avoid re-triggering the exact re-render-storm this fix removes, but `getEventDisplayState(item, now)` itself must always use the TRUE unrounded `now` — rounding that value would delay LIVE/COMPLETED transitions by up to 59s, flagged as an explicit task-level warning, not just a plan footnote). `analyze` ran PASS: 0 CRITICAL/HIGH/MEDIUM findings, 1 cosmetic LOW (intentional wording overlap between plan.md Risk Assessment and tasks.md T005, not a defect). Full FR-001..FR-010 and DR-1..DR-6 traceability confirmed; tasks.md = 13 tasks / 5 phases. Artifacts staged via `git add`, not committed — pipeline/orchestrator owns the signed commit after Checkpoint 2.
— discovered during `/blue.plan BFF-127`
